"use client";

import Link from "next/link";
import { motion } from "framer-motion";

const baseVariants = {
  initial: { opacity: 0, y: 20, scale: 0.98 },
  animate: { opacity: 1, y: 0, scale: 1 },
  exit: { opacity: 0, y: 20, scale: 0.98 }
};

type RecommendedItem = {
  id: string;
  name: string;
  house: string;
  description: string;
  score: number; // 0–1
  vibeTags: string[];
  vibeSentence: string;
  wearingScenario: string;
};

type Props = {
  items: RecommendedItem[];
};

export default function ResultsPageClient({ items }: Props) {
  const primary = items[0];

  return (
    <div className="max-w-6xl mx-auto px-4 py-6 md:py-10">
      {/* Top nav */}
      <header className="flex items-center justify-between mb-6 md:mb-8">
        <Link
          href="/quiz"
          className="flex items-center gap-2 text-xs text-slate-400 hover:text-amberLux"
        >
          ← Adjust answers
        </Link>
        <nav className="hidden md:flex items-center gap-6 text-xs uppercase text-slate-400">
          <Link href="/" className="hover:text-amberLux transition">
            Discover
          </Link>
          <Link href="/quiz" className="hover:text-amberLux transition">
            AI Scent Stylist
          </Link>
          <Link href="/checkout" className="hover:text-amberLux transition">
            Cart
          </Link>
        </nav>
      </header>

      <motion.main
        variants={baseVariants}
        initial="initial"
        animate="animate"
        transition={{ duration: 0.45, ease: "easeOut" }}
        className="space-y-8 md:space-y-10"
      >
        {/* Hero summary */}
        <section className="grid md:grid-cols-[3fr,2.2fr] gap-6 items-start">
          <div className="space-y-4 md:space-y-5">
            <p className="text-[0.65rem] uppercase tracking-[0.24em] text-slate-500">
              AI Scent Stylist — Results
            </p>
            <h1 className="text-2xl md:text-4xl font-light text-slate-50">
              These are the scents that feel{" "}
              <span className="bg-gradient-to-r from-amberLux to-softGold bg-clip-text text-transparent">
                closest to your current vibe.
              </span>
            </h1>
            {primary && (
              <p className="text-sm md:text-base text-slate-300">
                Your strongest signal leans towards{" "}
                <span className="text-amberLux font-medium">
                  {primary.vibeSentence}
                </span>{" "}
                — something that feels like {primary.wearingScenario.toLowerCase()}.
              </p>
            )}
            <p className="text-xs md:text-sm text-slate-400 max-w-xl">
              This is a mock UX skeleton: in production, the copy here will be
              dynamically generated by AI based on your quiz answers,
              describing your scent persona in a cinematic, emotion-first way.
            </p>
          </div>

          {/* Moodboard block */}
          <motion.div
            className="relative rounded-3xl bg-gradient-to-br from-fog via-slate-900 to-black p-4 md:p-6 shadow-lux-soft overflow-hidden"
            initial={{ opacity: 0, y: 24, scale: 0.96 }}
            animate={{ opacity: 1, y: 0, scale: 1 }}
            transition={{ duration: 0.5, delay: 0.08 }}
          >
            <div className="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(248,191,119,0.24),_transparent_60%)] opacity-90" />
            <div className="relative space-y-3">
              <p className="text-xs uppercase tracking-[0.22em] text-slate-300">
                Visual Moodboard
              </p>
              <p className="text-xs text-slate-200">
                Imagine a collage of soft lamplight, heavy fabrics, glassware,
                city windows at night and the kinds of hands, jewelry and rooms
                that match your answers. In the real build, this becomes a
                swipeable moodboard powered by your AI scent persona.
              </p>
              <div className="mt-3 grid grid-cols-3 gap-2">
                <div className="h-20 rounded-2xl bg-gradient-to-br from-slate-100/20 via-slate-400/20 to-amberLux/40" />
                <div className="h-20 rounded-2xl bg-gradient-to-br from-slate-900/80 via-slate-700/80 to-amberLux/30" />
                <div className="h-20 rounded-2xl bg-gradient-to-br from-amberLux/60 via-slate-300/10 to-slate-950" />
              </div>
            </div>
          </motion.div>
        </section>

        {/* Primary recommendation card */}
        {primary && (
          <section className="space-y-4 md:space-y-5">
            <div className="flex items-center justify-between gap-3">
              <h2 className="text-lg md:text-xl font-light text-slate-50 flex items-center gap-2">
                <span className="text-xs px-2 py-1 rounded-full border border-amberLux/70 text-amberLux tracking-[0.18em] uppercase">
                  #1 Match
                </span>
                <span>{primary.name}</span>
                <span className="text-[0.7rem] text-slate-500 uppercase tracking-[0.2em]">
                  {primary.house}
                </span>
              </h2>
              <p className="hidden md:block text-[0.7rem] text-slate-500">
                Match score:{" "}
                <span className="text-amberLux">
                  {(primary.score * 100).toFixed(0)}%
                </span>
              </p>
            </div>

            <div className="grid md:grid-cols-[2.2fr,2fr] gap-5 items-start">
              {/* Bottle + tags */}
              <div className="space-y-4">
                <div className="relative rounded-3xl bg-gradient-to-br from-slate-200/10 via-slate-50/5 to-amberLux/15 h-64 md:h-80 shadow-lux-soft overflow-hidden">
                  <div className="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.5),_transparent_55%)] mix-blend-screen" />
                  <div className="absolute inset-0 flex items-end justify-center pb-8">
                    <div className="h-44 w-22 md:h-56 md:w-24 rounded-3xl bg-gradient-to-b from-slate-100 to-slate-500 shadow-[0_25px_70px_rgba(0,0,0,0.85)] relative">
                      <div className="absolute inset-x-4 top-6 h-6 rounded-full bg-amberLux/20 blur-md" />
                      <div className="absolute bottom-6 left-1/2 -translate-x-1/2 text-[0.55rem] tracking-[0.3em] uppercase text-slate-900 text-center">
                        {primary.name}
                      </div>
                    </div>
                  </div>
                </div>
                <div className="flex flex-wrap gap-2">
                  {primary.vibeTags.map(tag => (
                    <span
                      key={tag}
                      className="px-3 py-1 rounded-full bg-fog/80 border border-slate-700 text-xs text-slate-200"
                    >
                      {tag}
                    </span>
                  ))}
                </div>
              </div>

              {/* Explanation */}
              <div className="space-y-3 md:space-y-4">
                <p className="text-sm md:text-base text-slate-300">
                  {primary.description}
                </p>

                <div className="space-y-2 text-xs md:text-sm text-slate-300">
                  <div>
                    <p className="text-[0.7rem] uppercase tracking-[0.2em] text-slate-500 mb-1">
                      Why this matches you
                    </p>
                    <p>{primary.vibeSentence}</p>
                  </div>
                  <div>
                    <p className="text-[0.7rem] uppercase tracking-[0.2em] text-slate-500 mb-1">
                      When to wear it
                    </p>
                    <p>{primary.wearingScenario}</p>
                  </div>
                  <div>
                    <p className="text-[0.7rem] uppercase tracking-[0.2em] text-slate-500 mb-1">
                      How it will feel on you
                    </p>
                    <p className="text-slate-400">
                      In a real AI flow, this paragraph would be generated
                      specifically for you, tying your answers (music, outfits,
                      social energy) to the emotional vibe of this perfume.
                    </p>
                  </div>
                </div>

                <div className="pt-2 flex flex-col sm:flex-row gap-3">
                  <Link
                    href={`/product/${primary.id}`}
                    className="inline-flex items-center justify-center rounded-3xl px-5 py-3 text-sm font-medium bg-gradient-to-r from-amberLux to-softGold text-ink shadow-lux-soft hover:opacity-95 transition"
                  >
                    View decants for {primary.name}
                  </Link>
                  <Link
                    href="/checkout"
                    className="inline-flex items-center justify-center rounded-3xl px-5 py-3 text-sm border border-slate-700/80 text-slate-200 hover:border-amberLux/70 hover:text-amberLux transition bg-fog/40 backdrop-blur-xs"
                  >
                    Save all three to cart (future)
                  </Link>
                </div>
              </div>
            </div>
          </section>
        )}

        {/* Secondary recommendations */}
        {items.length > 1 && (
          <section className="space-y-4">
            <div className="flex items-center justify-between">
              <h2 className="text-sm md:text-base uppercase tracking-[0.22em] text-slate-400">
                Supporting matches
              </h2>
              <p className="text-[0.7rem] text-slate-500">
                These still fit your vibe, but in slightly different moods.
              </p>
            </div>
            <div className="grid md:grid-cols-2 gap-4">
              {items.slice(1).map((item, index) => (
                <motion.div
                  key={item.id}
                  className="rounded-3xl bg-fog/80 border border-slate-800 p-4 md:p-5 flex flex-col justify-between shadow-lux-soft/40"
                  whileHover={{ y: -4 }}
                  transition={{ type: "spring", stiffness: 250, damping: 18 }}
                >
                  <div className="flex items-center justify-between mb-3">
                    <div className="space-y-1">
                      <p className="text-[0.65rem] uppercase tracking-[0.24em] text-slate-500">
                        #{index + 2} match
                      </p>
                      <p className="text-sm md:text-base text-slate-50">
                        {item.name}
                      </p>
                      <p className="text-[0.7rem] text-slate-500 uppercase tracking-[0.2em]">
                        {item.house}
                      </p>
                    </div>
                    <div className="text-right text-[0.7rem] text-slate-500">
                      Match:{" "}
                      <span className="text-amberLux">
                        {(item.score * 100).toFixed(0)}%
                      </span>
                    </div>
                  </div>
                  <p className="text-xs md:text-sm text-slate-300 mb-3">
                    {item.description}
                  </p>
                  <p className="text-xs text-slate-300 mb-2">
                    <span className="text-slate-500 uppercase tracking-[0.18em] text-[0.65rem]">
                      Why:
                    </span>{" "}
                    {item.vibeSentence}
                  </p>
                  <p className="text-xs text-slate-400 mb-3">
                    <span className="text-slate-500 uppercase tracking-[0.18em] text-[0.65rem]">
                      Best for:
                    </span>{" "}
                    {item.wearingScenario}
                  </p>
                  <div className="flex items-center justify-between mt-2">
                    <Link
                      href={`/product/${item.id}`}
                      className="text-xs text-amberLux hover:text-softGold transition"
                    >
                      View decants →
                    </Link>
                    <div className="flex flex-wrap gap-1">
                      {item.vibeTags.slice(0, 3).map(tag => (
                        <span
                          key={tag}
                          className="px-2 py-0.5 rounded-full bg-slate-900/80 border border-slate-700 text-[0.6rem] text-slate-300"
                        >
                          {tag}
                        </span>
                      ))}
                    </div>
                  </div>
                </motion.div>
              ))}
            </div>
          </section>
        )}

        {/* CTA bottom */}
        <section className="border-t border-slate-800 pt-6 md:pt-8 flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
          <div className="space-y-1">
            <p className="text-xs uppercase tracking-[0.24em] text-slate-500">
              Next steps
            </p>
            <p className="text-sm md:text-base text-slate-300">
              In the full build, you&apos;ll be able to fine-tune your vibe,
              swap one scent for another, and save multiple personas (work,
              date night, gym, travel).
            </p>
          </div>
          <div className="flex gap-3">
            <Link
              href="/quiz"
              className="inline-flex items-center justify-center rounded-3xl px-4 py-2.5 text-xs border border-slate-700/80 text-slate-200 hover:border-amberLux/70 hover:text-amberLux transition bg-fog/40 backdrop-blur-xs"
            >
              Retake quiz
            </Link>
            <Link
              href="/"
              className="inline-flex items-center justify-center rounded-3xl px-4 py-2.5 text-xs bg-slate-50/5 border border-slate-700 text-slate-200 hover:border-amberLux hover:text-amberLux transition"
            >
              Browse all vibes
            </Link>
          </div>
        </section>
      </motion.main>
    </div>
  );
}
